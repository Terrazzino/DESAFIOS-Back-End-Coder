<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
</head>
<body class="bg-black">
    <% if(productos.length>0){%>
    <div class="container mt-5 mx-auto bg-dark text-white p-4 rounded-3">
        <h2 class="p-3 text-center">Lista de productos</h2>
        <table class="table">
            <thead>
                <tr>
                    <th class="col-3 text-start text-white" scope="col">Producto</th>
                    <th class="col-3 text-center text-white" scope="col">Precio</th>
                    <th class="col-3 text-center text-white" scope="col">Imagen</th>
                </tr>
            </thead>
            <tbody>
                <% productos.forEach(function(prod){ %>
                <tr>
                    <td class="col-3 text-start text-white"><%=prod.nombre%></td>
                    <td class="col-3 text-center text-white"><%=prod.precio%></td>
                    <td class="col-3 text-center text-white"><img style="width:30px; height: auto" src="<%=prod.imagen%>"></td>
                </tr>
                <% });%>
            </tbody>
        </table>
        <a class="btn btn-primary d-flex justify-content-center" href="http://localhost:8080">Agregar más productos</a>
    </div>
    <%}else{%>
        <div class="container mt-5 mx-auto bg-dark text-white p-4 rounded-3">
            <h2 class="p-3 text-center text-danger">No hay productos agregados</h2>
            <a class="btn btn-primary d-flex justify-content-center" href="http://localhost:8080">Agregar productos</a>
        </div>
    <%}%>
</body>
</html>