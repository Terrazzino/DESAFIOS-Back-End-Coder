<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carritos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <script src="http://localhost:8080/socket.io/socket.io.js"></script>
</head>
<body class="bg-black text-whit">

    <div>
        <h1 class="text-white">CARRITOS DE PRODUCTOS</h1>
        <table class="table table-dark table-striped">
            <thead>
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Día</th>
                    <th scope="col">Lista de Productos</th>
                    <th>Eliminar</th>
                </tr>
            </thead>
            <tbody id="bodyTableCarr">
                {{#each carr}}
                <tr>
                    <td id="idProd">{{this.id}}</td>
                    <td>{{this.timestamp}}</td>
                    <td>{{this.productos}}</td>
                    <td><button id="btnDelete" class="btn btn-primary bg-danger ">X</button></td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
    <script src="./index.js"></script>
</body>
</html>